apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: keycloak-ldap-federation-crd
namespace: auth
labels:
- pairs:
    app.kubernetes.io/part-of: keycloak
    app.kubernetes.io/component: auth
    app.kubernetes.io/name: ldap-federation
resources:
- definition.yaml
- composition.yaml
